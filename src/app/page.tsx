function Home() {
  return (
    <h1>
      Aqui debe estar todo el codigo de las 4 api el GET,POST,PATCH,DELETE.,
      peticiones con axios a la api estas son las rutas /api/todo aqui (GET Y
      POST) /api/todo[id] aqui [PATCH Y DELETE] con axios
    </h1>
  );
}

export default Home;
